import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.solimanware.dawaware',
  appName: 'DawaWare',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
